# Findings from the walkthrough capture

Awkward behaviour the run surfaced. Skills were not changed.

## Board search failed, so every ➡️ was unsourced

`grill-ticket` says search the board before the first round: it settles Relations without a question, and a real ticket is the best source a ➡️ line can cite.

There is no JIRA in this session. Lookup failed. Neighbouring tickets were not invented. Relations was treated as none / lookup failed, and had to be asked.

That made the grill awkward in a specific way: on a greenfield request the ➡️ lines had nothing to cite, so they repeated "I have nothing to base a preference on." The delete-semantics pushback still ran. It just ran without the sourced suggestion the skill is written to prefer.

Same failure on the short grill and the bug. The short grill still reached a brief in one step — Relations was already in the opening — but the agent could not confirm "nothing this follows from" against a board.

## Boundary options vs adjacent features

On the Boundary branch, "lay out the options" would have meant naming bulk delete, deleting the project, or GDPR-erasure-as-a-button before the requester did. `smells.md` says not to raise adjacent features. The run asked Boundary open-ended and left ➡️ unsourced. The requester still named those three exclusions. The two instructions pull against each other on that branch; the open question is what survived.
