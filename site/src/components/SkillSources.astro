---
import { loadInventory } from '../lib/content';
import { PM_PLUGIN_ROOT } from '../lib/plugin-root';
import SkillSource from './SkillSource.astro';

const skills = loadInventory(PM_PLUGIN_ROOT).skills;
---

{
	skills.map((skill) => (
		<SkillSource name={skill.name} />
	))
}
